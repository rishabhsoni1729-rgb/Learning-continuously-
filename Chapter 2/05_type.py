a = "4.5"           # by assigning a to different datatype using type function will help to find which class is belongs to.

b = float(a)           # a but the type should be float
t = type(b)              # but the best part is i can change the datatype as well.

print(t)

c = 4.5
d = int(c)         # So using this [int()] function i converted float into int
t = type(d)

print(t)

# I Can Know about the type assign to variable 
e = "2.3"   # I can also change the datatype of e by using fucntion like , int() , flaot() or even str()
t = type(e)
print(t)

# in simple for know the type i can directly do this as well 

f = type(3.4)
print(f)